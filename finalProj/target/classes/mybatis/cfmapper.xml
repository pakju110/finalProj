<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cf">

	<select id="selectList" resultType="aliscf">
		select * from cf
	</select>

	<select id="selectDetail" resultType="aliscf" parameterType="aliscf">
		select * from cf where rest_id = #{rest_id}
	</select>

	<delete id="delete" parameterType="aliscf">
		delete from cf where rest_id = #{rest_id}
	</delete>


<!-- 	<delete id="modify" parameterType="aliscf">
		update inpay set user_address = #{user_address}, user_phone =
		#{user_phone} where rest_id = #{rest_id} 
	</delete>
	
	<delete id="grademodify" parameterType="aliscf">
		update inpay set grade = #{grade} where rest_id = #{rest_id} 
	</delete> -->
	
	
	<insert id="insertOne" parameterType="aliscf">
		insert into cf (rest_id, reg_date ,cf_sysimg,cf_oriimg,approval) values
		(#{rest_id},sysdate,#{cf_sysimg,jdbcType=VARCHAR},#{cf_oriimg,jdbcType=VARCHAR},'w')
	</insert>
	
	
	<!-- <insert id="insertTwo" parameterType="aliscf">
		<selectKey keyProperty="id" order="BEFORE" resultType="java.lang.Integer">
			select max(id)+1 from inpay
		</selectKey>
		insert into inpay (id, rest_id, reg_date, user_name,user_phone,
		user_pw, user_address,grade,sysfile,orifile) values
		(#{id},#{rest_id},sysdate,#{user_name},#{user_phone}, #{user_pw},
		#{user_address},'w',#{sysfile},#{orifile})

	</insert> -->





	<!-- <sql id="sch"> <where> <if test="what!=null and what!=''"> ${kind} 
		like '%'||#{what}||'%' </if> </where> </sql> <select id="selectListSch" resultType="UserVo" 
		parameterType="boardSch"> select * from inpay <include refid="sch"/> 
		</select> <sql id="sch2"> <if test="sss.what!=null and sss.what!=''"> ${sss.kind} 
		like '%'||#{sss.what}||'%' or </if> </sql> -->

	<!-- <select id="selectListSchList" resultType="UserVo" parameterType="boardSch"> 
		select * from inpay <where> <if test="list!=null"> <foreach collection="list" 
		item="sss"> <include refid="sch2"/> </foreach> 1!=1 </if> <if test="cnt!=null 
		and cnt!=0"> <if test="list!=null"> or </if> <![CDATA[ cnt >= #{cnt} ]]> 
		</if> </where> </select> -->




	<!-- <update id="fileDelete" parameterType="UserVo"> update inpay set 
		upfile = null where id = #{id} </update> -->




</mapper>